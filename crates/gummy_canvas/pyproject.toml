[build-system]
requires = ["maturin>=1.9.4,<2"]
build-backend = "maturin"

[tool.maturin]
module-name = "gummysnake.rust._canvas"
python-source = "../../src"
bindings = "pyo3"
features = ["extension-module"]
