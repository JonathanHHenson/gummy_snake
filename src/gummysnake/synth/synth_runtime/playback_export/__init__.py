"""Track-facing playback, export, asset metadata, and Sound conversion internals.

This intentionally avoids the name ``playback`` so the retained compatibility
module ``synth_runtime.playback`` cannot collide with a same-stem package.
"""
