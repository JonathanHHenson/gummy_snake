"""Self-contained ECS benchmark fixtures, oracles, and bounded workloads.

The implementation lives in sibling modules. This package intentionally provides no
barrel re-exports so static worker dispatch remains explicit and auditable.
"""
