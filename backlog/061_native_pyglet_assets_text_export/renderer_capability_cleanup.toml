[my_pbi]
title = "Clean up native Pyglet capability reporting after implementation"
description = '''
As a backend integrator,
I want renderer capabilities to accurately describe native Pyglet image, text, pixel, and export support,
So that sketches and tests can select backends predictably.
'''
acceptance_criteria = '''
Backend capabilities, renderer protocol implementations, errors, tests, and docs are updated so unsupported native Pyglet APIs are capability-gated only where they remain truly unsupported. Existing custom-backend extension points continue to work.
'''
priotity = 'medium'
status = 'TODO'

[my_pbi.task_1]
order = 1
status = 'TODO'
description = '''
Review `BackendCapabilities` and renderer protocol methods to decide whether image, text, export, and pixel support should be represented explicitly.
'''

[my_pbi.task_2]
order = 2
status = 'TODO'
description = '''
Replace obsolete `BackendCapabilityError` paths in `PygletRenderer` with native implementations as each capability lands.
'''

[my_pbi.task_3]
order = 3
status = 'TODO'
description = '''
Add regression tests that assert native Pyglet no longer raises capability errors for implemented APIs, including `save_canvas`.
'''
